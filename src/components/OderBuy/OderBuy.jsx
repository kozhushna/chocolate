import { ButtonClose } from 'components/ButtonIcon/ButtonClose';
import { StyledDiv } from './OderBuy.styled';

export const FormOderBuy = ({ action }) => {
  return (
    <StyledDiv>
      text
      <ButtonClose action={action} />
    </StyledDiv>
  );
};
